name: lavalo
description: A new Flutter project.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.11+11

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  cloud_firestore: ^0.13.0+1
  generic_bloc_provider: ^1.0.9
  firebase_auth: ^0.15.3
  firebase_storage: ^3.1.5
  google_sign_in: ^4.1.1
  carousel_slider: ^1.4.0
  mercado_pago: ^0.1.3
  intl: ^0.16.0
  percent_indicator: ^2.1.1+1
  flutter_launcher_icons: ^0.7.4
  url_launcher: ^5.4.1
  image_picker: ^0.6.3+4

flutter_icons:
  image_path: "assets/img/logoSolo.png"
  image_path_android: "assets/img/logoSolo.png"
  image_path_ios: "assets/img/logoSolo.png"
  android: true # can specify file name here e.g. "only-logo.png"
  ios: true # can specify file name here e.g. "only-logo.png"

dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/img/uno.jpg
    - assets/img/dos.jpg
    - assets/img/tres.jpg
    - assets/img/cuatro.jpg
    - assets/img/cinco.jpg
    - assets/img/cat-dribbble.gif
    - assets/img/avatar_male.gif
    - assets/img/avatar_female.png
    - assets/img/img1.jpg
    - assets/img/img2.jpg
    - assets/img/img3.jpg
    - assets/img/img4.jpg
    - assets/img/img5.jpg
    - assets/avatar.gif
    - assets/avatarend.gif

fonts:
  - family: Lato
    fonts:
        - asset: fonts/Lato-Regular.ttf